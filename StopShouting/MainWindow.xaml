<Window x:Class="StopShouting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:l="clr-namespace:StopShouting"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Stop Shouting"
        Width="800"
        Height="450">

    <Window.Resources>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="SectionLabelStyle" TargetType="Label">
            <Setter Property="Padding" Value="30,5" />
        </Style>

        <Style x:Key="EditorTextBoxStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="30,0" />
            <Setter Property="MinHeight" Value="100" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        </Style>

    </Window.Resources>

    <Grid Margin="0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->

        <TextBlock Grid.Row="0"
                   Style="{StaticResource HeaderTextStyle}"
                   Padding="0,10">
            Thank you for contacting the Void.
        </TextBlock>

        <TextBlock Grid.Row="1"
                   Style="{StaticResource HeaderTextStyle}"
                   Padding="0,5">
            Your screams are dear to us.
        </TextBlock>

        <!-- Input -->

        <Label Grid.Row="2"
               Style="{StaticResource SectionLabelStyle}">
            Text to translate
        </Label>

        <TextBox x:Name="InputTextBox"
            Grid.Row="3"
                 Style="{StaticResource EditorTextBoxStyle}"
                 l:TextBoxHelper.Placeholder="Paste text here" />

        <!-- Action -->

        <Button Grid.Row="4"
                Width="100"
                Margin="30,10,30,5"
                HorizontalAlignment="Right"
                Click="SubmitButton_Click"
                Content="Submit">
        </Button>

        <!-- Output -->

        <Label Grid.Row="5"
               Style="{StaticResource SectionLabelStyle}"
               >
            Text de-screamed
        </Label>

        <Grid Grid.Row="6"
              Margin="30,0">

            <TextBox x:Name="OutputTextBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     l:TextBoxHelper.Placeholder="Output text with screams removed"
                     />

            <Button Content=" 📄 "
                    Background="Transparent"
                    BorderBrush="Gray"
                    BorderThickness=".75"
                    Width="30"
                    Height="25"
                    Margin="0,5,5,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Click="CopyButton_Click"
                    ToolTip="Copy" 
                    />
        </Grid>

        <Border x:Name="CopiedToast"
                Background="#333333"
                CornerRadius="6"
                Padding="12,6"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,15"
                Visibility="Collapsed">

            <TextBlock Text="Copied!"
                       Foreground="White"
                       FontWeight="SemiBold"/>

        </Border>
    </Grid>
</Window>